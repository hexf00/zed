diff -ruN a/Cargo.toml b/Cargo.toml
--- a/Cargo.toml	2026-09-21 02:51:04.907375295 +0800
+++ b/Cargo.toml	2026-09-21 02:51:25.752312300 +0800
@@ -3,7 +3,7 @@
 edition = "2024"
 name = "sccache"
 rust-version = "1.91.0"
-version = "0.18.0"
+version = "0.18.0-fork.1"
 
 categories = ["command-line-utilities", "development-tools::build-utils"]
 description = "Sccache is a ccache-like tool. It is used as a compiler wrapper and avoids compilation when possible. Sccache has the capability to utilize caching in remote storage environments, including various cloud storage options, or alternatively, in local storage."
diff -ruN a/src/compiler/rust.rs b/src/compiler/rust.rs
--- a/src/compiler/rust.rs	2026-09-21 02:51:04.951377272 +0800
+++ b/src/compiler/rust.rs	2026-09-21 02:51:25.752312300 +0800
@@ -235,7 +235,7 @@
     LazyLock::new(|| ["link", "metadata", "dep-info"].iter().copied().collect());
 
 /// Version number for cache key.
-const CACHE_VERSION: &[u8] = b"6";
+const CACHE_VERSION: &[u8] = b"7";
 
 /// Get absolute paths for all source files and env-deps listed in rustc's dep-info output.
 async fn get_source_files_and_env_deps<T>(
@@ -1448,7 +1448,7 @@
             .iter()
             .map(|e| cwd.join(e))
             .collect::<Vec<_>>();
-        let extern_hashes = hash_all(&abs_externs, pool);
+        let extern_hashes = hash_all_externs(&abs_externs, pool);
         // Hash the contents of the staticlibs listed on the commandline.
         trace!(
             "[{}]: hashing {} staticlibs",
diff -ruN a/src/util.rs b/src/util.rs
--- a/src/util.rs	2026-09-21 02:51:04.963377813 +0800
+++ b/src/util.rs	2026-09-21 03:07:07.889055530 +0800
@@ -455,6 +455,195 @@
     Ok(())
 }
 
+/// Calculate the digest of each rustc `--extern` dependency file on
+/// background threads in `pool`.
+///
+/// MSVC-linked images (proc-macro and dylib `dll`s) carry link-time metadata
+/// that changes on every link even when all inputs are identical: the COFF
+/// and debug-directory `TimeDateStamp` fields, the CodeView RSDS GUID and
+/// PDB age, the export-directory timestamp, and the `REPRO` payload hashes.
+/// Hashing those bytes keys every dependent crate's cache entry to the wall
+/// clock, so they are zeroed before hashing. Non-PE files (rlibs, rmeta) are
+/// hashed as-is, and any PE parsing surprise falls back to the plain file
+/// digest: normalization must never fail a compilation.
+pub async fn hash_all_externs(
+    files: &[PathBuf],
+    pool: &tokio::runtime::Handle,
+) -> Result<Vec<String>> {
+    let start = time::Instant::now();
+    let count = files.len();
+    let iter = files.iter().map(|f| {
+        let f = f.clone();
+        let pool = pool.clone();
+        async move {
+            match pool
+                .spawn_blocking(move || normalized_extern_image_digest(&f))
+                .await?
+            {
+                Some(digest) => Ok(digest),
+                None => Digest::file(&f, &pool).await,
+            }
+        }
+    });
+    let hashes = futures::future::try_join_all(iter).await?;
+    trace!(
+        "Hashed {} extern files in {}",
+        count,
+        fmt_duration_as_secs(&start.elapsed())
+    );
+    Ok(hashes)
+}
+
+/// Digest of a `--extern` file with PE link-time metadata normalized to
+/// zero, or `None` when the file is not a PE image or cannot be normalized.
+fn normalized_extern_image_digest(path: &Path) -> Result<Option<String>> {
+    let mut file = fs::File::open(path)
+        .with_context(|| format!("Failed to open file for hashing: {:?}", path))?;
+    let mut magic = [0u8; 2];
+    if file.read_exact(&mut magic).is_err() || &magic != b"MZ" {
+        return Ok(None);
+    }
+    let mut data = Vec::new();
+    file.read_to_end(&mut data)
+        .with_context(|| format!("Failed to read file for hashing: {:?}", path))?;
+    if normalize_pe_volatile_bytes(&mut data).is_err() {
+        return Ok(None);
+    }
+    let mut digest = Digest::new();
+    digest.update(&data);
+    Ok(Some(digest.finish()))
+}
+
+/// Zero the link-time-volatile bytes of a PE image in place.
+///
+/// Covers every field observed (or documented) to vary between links of
+/// identical inputs by MSVC link.exe: the COFF header timestamp, the export
+/// directory and import descriptor timestamps, the debug directory entry
+/// timestamps, the CodeView RSDS GUID + PDB age, `REPRO` payload hashes, and
+/// the bound import directory. Returns `Err` when the buffer is not a PE
+/// image with a layout this function understands.
+fn normalize_pe_volatile_bytes(data: &mut [u8]) -> Result<()> {
+    fn u16_at(data: &[u8], off: usize) -> Result<u16> {
+        Ok(u16::from_le_bytes(
+            data.get(off..off + 2)
+                .ok_or_else(|| anyhow!("PE header truncated"))?
+                .try_into()
+                .unwrap(),
+        ))
+    }
+    fn u32_at(data: &[u8], off: usize) -> Result<u32> {
+        Ok(u32::from_le_bytes(
+            data.get(off..off + 4)
+                .ok_or_else(|| anyhow!("PE header truncated"))?
+                .try_into()
+                .unwrap(),
+        ))
+    }
+    fn zero(data: &mut [u8], off: usize, len: usize) -> Result<()> {
+        data.get_mut(off..off + len)
+            .ok_or_else(|| anyhow!("PE range out of bounds"))
+            .map(|b| b.fill(0))
+    }
+    /// Map an RVA to a file offset via the section table.
+    fn rva_to_offset(sections: &[(u32, u32, u32, u32)], rva: u32) -> Option<usize> {
+        for &(vaddr, vsize, rsize, raddr) in sections {
+            if rva >= vaddr && rva < vaddr.saturating_add(vsize.max(rsize)) {
+                return Some(raddr as usize + (rva - vaddr) as usize);
+            }
+        }
+        None
+    }
+
+    let e_lfanew = u32_at(data, 0x3c)? as usize;
+    if data.get(e_lfanew..e_lfanew + 4) != Some(b"PE\0\0") {
+        bail!("not a PE image");
+    }
+    let coff = e_lfanew + 4;
+    // COFF header: Machine(2) NumberOfSections(2) TimeDateStamp(4) ...
+    zero(data, coff + 4, 4)?;
+    let nsections = u16_at(data, coff + 2)? as usize;
+    let opt_size = u16_at(data, coff + 16)? as usize;
+    let opt = coff + 20;
+    let magic = u16_at(data, opt)?;
+    let (nrva_off, dd_off) = match magic {
+        0x20b => (opt + 108, opt + 112), // PE32+
+        0x10b => (opt + 92, opt + 96),   // PE32
+        _ => bail!("unknown optional header magic"),
+    };
+    let nrva = u32_at(data, nrva_off)? as usize;
+    let sections_off = opt + opt_size;
+    let mut sections = Vec::with_capacity(nsections);
+    for i in 0..nsections {
+        let s = sections_off + i * 40;
+        // Section header: Name(8) VirtualSize(4) VirtualAddress(4)
+        // SizeOfRawData(4) PointerToRawData(4).
+        let vsize = u32_at(data, s + 8)?;
+        let vaddr = u32_at(data, s + 12)?;
+        let rsize = u32_at(data, s + 16)?;
+        let raddr = u32_at(data, s + 20)?;
+        sections.push((vaddr, vsize, rsize, raddr));
+    }
+    let dir = |i: usize| -> Result<Option<(usize, usize)>> {
+        if i >= nrva {
+            return Ok(None);
+        }
+        let rva = u32_at(data, dd_off + i * 8)?;
+        let size = u32_at(data, dd_off + i * 8 + 4)? as usize;
+        if rva == 0 || size == 0 {
+            return Ok(None);
+        }
+        match rva_to_offset(&sections, rva) {
+            Some(off) => Ok(Some((off, size))),
+            None => Ok(None),
+        }
+    };
+    // Export directory (index 0): TimeDateStamp at +4.
+    if let Some((off, _)) = dir(0)? {
+        zero(data, off + 4, 4)?;
+    }
+    // Import directory (index 1): zero TimeDateStamp of each
+    // IMAGE_IMPORT_DESCRIPTOR (20 bytes, ts at +4, until an all-zero entry).
+    if let Some((off, size)) = dir(1)? {
+        let mut i = 0;
+        while i + 20 <= size {
+            if data[off + i..off + i + 20].iter().all(|&b| b == 0) {
+                break;
+            }
+            zero(data, off + i + 4, 4)?;
+            i += 20;
+        }
+    }
+    // Debug directory (index 6): zero each entry's TimeDateStamp, plus the
+    // CodeView RSDS GUID+PDB age and the whole REPRO payload.
+    if let Some((off, size)) = dir(6)? {
+        let mut i = 0;
+        while i + 28 <= size {
+            zero(data, off + i + 4, 4)?;
+            let entry_type = u32_at(data, off + i + 12)?;
+            let payload_size = u32_at(data, off + i + 16)? as usize;
+            let payload_off = u32_at(data, off + i + 24)? as usize;
+            match entry_type {
+                // IMAGE_DEBUG_TYPE_CODEVIEW
+                2 if payload_size >= 24 => {
+                    if data.get(payload_off..payload_off + 4) == Some(b"RSDS") {
+                        // Zero GUID (4..20) and PDB age (20..24).
+                        zero(data, payload_off + 4, 20)?;
+                    }
+                }
+                // IMAGE_DEBUG_TYPE_REPRO: whole payload is an input hash.
+                16 => zero(data, payload_off, payload_size)?,
+                _ => {}
+            }
+            i += 28;
+        }
+    }
+    // Bound import directory (index 11): metadata-only optimization, zero it.
+    if let Some((off, size)) = dir(11)? {
+        zero(data, off, size)?;
+    }
+    Ok(())
+}
+
 /// Format `duration` as seconds with a fractional component.
 pub fn fmt_duration_as_secs(duration: &Duration) -> String {
     format!("{}.{:03} s", duration.as_secs(), duration.subsec_millis())
@@ -1530,11 +1719,94 @@
     executable.to_path_buf()
 }
 
-#[cfg(test)]
 mod tests {
-    use super::{Digest, OsStrExt, TimeMacroFinder, resolve_compiler_avoiding_wrapper};
+    use super::{Digest, OsStrExt, TimeMacroFinder, normalize_pe_volatile_bytes, resolve_compiler_avoiding_wrapper};
     use std::ffi::{OsStr, OsString};
     use std::path::Path;
+    #[test]
+    fn test_normalize_pe_volatile_bytes() {
+        // Minimal PE32+ image: DOS header, COFF, optional header with the
+        // full data directory array, one section holding export/debug/bound
+        // directories and RSDS/REPRO payloads.
+        let e_lfanew = 0x80usize;
+        let coff = e_lfanew + 4;
+        let opt = coff + 20;
+        let opt_size = 112usize + 16 * 8; // PE32+ header + full data dirs
+        let section_off = opt + opt_size;
+        let rdata_rva = 0x1000u32;
+        let rdata_off = 0x400usize;
+        let total = rdata_off + 0x200;
+        let mut pe = vec![0u8; total];
+        pe[0..2].copy_from_slice(b"MZ");
+        pe[0x3c..0x40].copy_from_slice(&(e_lfanew as u32).to_le_bytes());
+        pe[e_lfanew..e_lfanew + 4].copy_from_slice(b"PE\0\0");
+        pe[coff..coff + 2].copy_from_slice(&0x8664u16.to_le_bytes());
+        pe[coff + 2..coff + 4].copy_from_slice(&1u16.to_le_bytes());
+        pe[coff + 4..coff + 8].copy_from_slice(&0x6ab017f8u32.to_le_bytes());
+        pe[coff + 16..coff + 18].copy_from_slice(&(opt_size as u16).to_le_bytes());
+        pe[opt..opt + 2].copy_from_slice(&0x20bu16.to_le_bytes());
+        let nrva_off = opt + 108;
+        let dd_off = opt + 112;
+        pe[nrva_off..nrva_off + 4].copy_from_slice(&16u32.to_le_bytes());
+        let set_dir = |i: usize, rva: u32, size: u32| {
+            pe[dd_off + i * 8..dd_off + i * 8 + 4].copy_from_slice(&rva.to_le_bytes());
+            pe[dd_off + i * 8 + 4..dd_off + i * 8 + 8].copy_from_slice(&size.to_le_bytes());
+        };
+        set_dir(0, rdata_rva + 0x40, 0x40); // export
+        set_dir(6, rdata_rva, 56); // debug: CODEVIEW + REPRO entries
+        set_dir(11, rdata_rva + 0x100, 0x20); // bound import
+        pe[section_off + 8..section_off + 12].copy_from_slice(&0x200u32.to_le_bytes());
+        pe[section_off + 12..section_off + 16].copy_from_slice(&rdata_rva.to_le_bytes());
+        pe[section_off + 16..section_off + 20].copy_from_slice(&0x200u32.to_le_bytes());
+        pe[section_off + 20..section_off + 24].copy_from_slice(&(rdata_off as u32).to_le_bytes());
+        // Export directory timestamp.
+        let exp_off = rdata_off + 0x40;
+        pe[exp_off + 4..exp_off + 8].copy_from_slice(&0x1234u32.to_le_bytes());
+        // Debug entry 0: CODEVIEW with an RSDS GUID + PDB age payload.
+        let dbg = rdata_off;
+        pe[dbg + 4..dbg + 8].copy_from_slice(&0x6ab017f8u32.to_le_bytes());
+        pe[dbg + 12..dbg + 16].copy_from_slice(&2u32.to_le_bytes());
+        pe[dbg + 16..dbg + 20].copy_from_slice(&24u32.to_le_bytes());
+        pe[dbg + 24..dbg + 28].copy_from_slice(&((rdata_off + 0x80) as u32).to_le_bytes());
+        let rsds = rdata_off + 0x80;
+        pe[rsds..rsds + 4].copy_from_slice(b"RSDS");
+        for b in pe[rsds + 4..rsds + 24].iter_mut() {
+            *b = 0xAA;
+        }
+        // Debug entry 1: REPRO input-hash payload.
+        pe[dbg + 32..dbg + 36].copy_from_slice(&0x6ab017f9u32.to_le_bytes());
+        pe[dbg + 44..dbg + 48].copy_from_slice(&16u32.to_le_bytes());
+        pe[dbg + 48..dbg + 52].copy_from_slice(&8u32.to_le_bytes());
+        pe[dbg + 56..dbg + 60].copy_from_slice(&((rdata_off + 0xA0) as u32).to_le_bytes());
+        let repro = rdata_off + 0xA0;
+        for b in pe[repro..repro + 8].iter_mut() {
+            *b = 0xBB;
+        }
+        // Bound import directory content.
+        for b in pe[rdata_off + 0x100..rdata_off + 0x120].iter_mut() {
+            *b = 0xCC;
+        }
+        // Content that must survive normalization.
+        pe[rdata_off + 0x140..rdata_off + 0x144].copy_from_slice(&[1, 2, 3, 4]);
+
+        normalize_pe_volatile_bytes(&mut pe).expect("normalize ok");
+        assert_eq!(pe[coff + 4..coff + 8], 0u32.to_le_bytes());
+        assert_eq!(pe[exp_off + 4..exp_off + 8], 0u32.to_le_bytes());
+        assert_eq!(pe[dbg + 4..dbg + 8], 0u32.to_le_bytes());
+        assert_eq!(pe[dbg + 32..dbg + 36], 0u32.to_le_bytes());
+        assert!(pe[rsds + 4..rsds + 24].iter().all(|&b| b == 0));
+        assert!(pe[repro..repro + 8].iter().all(|&b| b == 0));
+        assert!(pe[rdata_off + 0x100..rdata_off + 0x120]
+            .iter()
+            .all(|&b| b == 0));
+        assert_eq!(&pe[rdata_off + 0x140..rdata_off + 0x144], &[1, 2, 3, 4]);
+    }
+
+    #[test]
+    fn test_normalize_rejects_non_pe() {
+        let mut data = b"MZ this is not a PE image".to_vec();
+        assert!(normalize_pe_volatile_bytes(&mut data).is_err());
+    }
 
     #[test]
     fn test_resolve_compiler_avoiding_ccache_filters_path() {
